services:
  helpdesk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: helpdesk
    ports:
      - "8085:8085" # HTTP Server
      - "8086:8086" # gRPC Server
    restart: always
