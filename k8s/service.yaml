apiVersion: v1
kind: Service
metadata:
  name: helpdesk
spec:
  selector:
    app: helpdesk
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: grpc
      port: 8081
      targetPort: grpc
